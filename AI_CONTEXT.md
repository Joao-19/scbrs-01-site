# AI Context Entry Point

This repository includes a dedicated AI documentation set in `ai_context/`.

Start here:
- `ai_context/index.md`

Important:
- `ai_context/` is English-only by convention.
- Human-facing business rules are in `docs/` (Portuguese).
